namespace SalesOrderPlugin.Enum
{
    public enum HandlingEnum
    {
        NoAction = 1,
        Release,
        Cancel
    }

    public enum StateEnum
    {
        Open = 1,
        Released,
        Canceled
    }
}